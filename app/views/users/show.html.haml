%p#notice= notice
.profile-container
  .profile-avatar
    %img{src: @user.avatar_url}
  .profile-username
    %strong Username:
    = @user.username
  .profile-email
    %strong Email:
    = @user.email
= link_to 'Edit', edit_user_path(@user)
|
= link_to 'Back', users_path
|
= link_to "Logout", logout_path, method: :post

= react_component 'Experience', { data: @experience }
