%p#notice= notice
.profile-container
  .profile-avatar
    %img{src: @user.avatar_url}
  .profile-username
    %strong Username:
    = @user.username
  .profile-eramail
    %strong Email:
    = @user.email
= link_to 'Edit', edit_user_path(@user)
|
= link_to 'Back', users_path
|
= link_to "Logout", logout_path, method: :post
%p
  = link_to "Delete Account", user_path(id: current_user.id), method: :delete
