== README
To try it out:

1. Clone to your local machine

2. bundle install

3. bundle execute rake db:create

4. bundle execute rake db:migrate

5. bundle execute rake db:seed

6. rails s

***TESTS***

After step 3 above, run: bundle exec rake db:test:prepare


<tt>rake doc:app</tt>.
